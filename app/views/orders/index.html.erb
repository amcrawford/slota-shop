<h2>Your Past Orders</h2>

<% @orders.each do |order| %>
  <h4>Order <%= order.id %></h4>
  Ordered On:<br>
  <%= order.created_at.strftime("%B %d, %Y") %> | <%= link_to "View Order Details", user_order_path(@user, order) %><br><br>
<% end %>
