<div class="col m4">
  <div class="card medium">
    <div class="card-image">
      <%= image_tag chip.image.url, id: "chip-picture" %>
    </div>
    <div class="card-content" id="<%= chip.slug%>">
      <%= render partial: 'admin/chips/chip_layout', locals: {chip: chip, source: "index"} %>
    </div>
  </div>
</div>
