<b><%= chip.name %></b>
<p><%= number_to_currency chip.price %></p>
 <% if chip.status == "Available" %>
 <!-- <%# if chip.available? %> -->
  <p><%= button_to "Add to Cart", cart_chips_path(chip_id: chip.id), class: "btn"%></p>
<% else %>
  <p class="btn disabled"> Item is Retired </p>
<% end %>
<p><%= chip.description_type(source) %></p>

<%#= display_add_to_cart(chip) %>
